import { PaginationLoadDataPipe } from './pagination-load-data.pipe';

describe('PaginationLoadDataPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginationLoadDataPipe();
    expect(pipe).toBeTruthy();
  });
});
